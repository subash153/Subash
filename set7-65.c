#include<stdio.h>
void main()
{
    int n,a[10],i,k;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    
    }
    for(k=0;k<n;k++)
    {
        if(a[k]<n)
        {
            b[k]=a[k];
        
        
    printf("%d",b[k]);
        }
}
    
}
